import UploadPage from "./upload/page";

export default function HomePage() {
  return (
    <div>
      {/* Renders the upload page content as the site's home page */}
      <UploadPage />
    </div>
  );
}