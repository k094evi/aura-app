version: "3.9"

services:
  api:
    build: .
    container_name: aura_parser
    ports:
      - "8000:8000"
    volumes:
      - .:/app        # live reload during development
    restart: unless-stopped