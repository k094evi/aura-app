# NOTE: torch / transformers / sentence-transformers do not reliably ship
# prebuilt wheels for brand-new Python releases (e.g. 3.14) for months
# after that Python version comes out — pip then tries to build from
# source and fails, or fails to resolve at all. Rather than chasing
# version pins, use Python 3.11 or 3.12 for this project's venv:
#   py -3.12 -m venv .venv
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9
pydantic
pydantic-settings
python-dotenv==1.0.1
pdfplumber==0.11.0
python-docx==1.1.2
#spacy==3.7.4
scikit-learn
transformers
sentence-transformers
torch
httpx==0.27.0
supabase
email-validator          # required by EmailStr in app/models/auth_schemas.py
python-magic-bin; sys_platform == "win32"   # optional stronger MIME detection; file_handler.py falls back to extension-checking without it

